# 潮汐資料無法正確取得 — 問題診斷

> 對象檔案：`index.html`（DIVIN'IN 潛靜 Fun Dive 行程頁）
> 資料來源：中央氣象署 Open Data API — **F-A0021-001 未來 1 個月潮汐預報**
> 診斷日期：2026-06-11

---

## 結論摘要

「抓不到正確潮汐」其實分成兩類問題：

- **根本沒去抓**（問題 1）— 主因
- **就算去抓也會失敗**（問題 2、3、4）

| # | 問題 | 類型 | 影響 | 狀態 |
|---|------|------|------|------|
| 1 | API Key 空白 | 設定值 | 完全不發送請求，只顯示寫死的手動值 | ⏸️ 依需求保留空白 |
| 2 | LocationName 是不存在的地名 | 程式設定 | API 比對不到地點，回空陣列 | ✅ 已修正 |
| 3 | 回應解析少進一層 `.Location` | 程式 bug | 即使拿到資料也解析不出來 | ✅ 已修正 |
| 4 | CONFIG 日期已過期 | 設定值 | 超出「未來 1 個月」預報範圍，比對不到 | ⚠️ 使用時再改 |

---

## 問題 1：API Key 沒填，根本沒發出請求（主因）

`CONFIG.cwaApiKey` 是空字串，而啟動自動抓取的判斷式：

```js
if (CONFIG.cwaApiKey) {   // 空字串 = false → 整段不執行
  fetchTideData(...)
}
```

因此 `fetchTideData()` 從頭到尾**沒被呼叫過**。畫面上看到的 04:07 / 10:05 / 15:43 是 `CONFIG.tide` 寫死的手動備援值，不是真實潮汐。

- **位置**：`index.html` — `CONFIG.cwaApiKey`、`if (CONFIG.cwaApiKey)` 判斷區塊
- **本質**：不是「抓錯」，而是**完全沒去抓**
- **處理**：需到 <https://opendata.cwa.gov.tw/> 申請免費授權碼填入，自動抓取才會啟動（本次依需求**保留空白**）

---

## 問題 2：兩個潛點的 LocationName 是「不存在的地名」（已修正）

F-A0021-001 的 `LocationName` 必須是氣象署的**鄉鎮名稱**，以完全比對篩選，地名不對就回空陣列。潮汐預報只到鄉鎮層級，沒有細到單一潛點。

| 潛點 | 修改前（錯誤） | 修改後（正確） | 說明 |
|------|---------------|---------------|------|
| 蝙蝠洞 | `新北市瑞芳區` | `瑞芳區` | 多了縣市前綴，API 只要鄉鎮名 |
| 龍洞3號 | `潛點龍洞灣水域 B 區北側` | `貢寮區` | 原為不存在的自訂名稱 |
| 蚊子坑 | `潛點龍洞南口水域 C 區` | `貢寮區` | 原為不存在的自訂名稱 |

- **位置**：`index.html` — `WEATHER_MAP` 的 `tideLocation` 欄位
- **處理**：已改為氣象署實際鄉鎮名稱（去縣市前綴）

---

## 問題 3：回應解析少進一層 `.Location`（已修正）

CWA 新版回應結構為：

```
records.TideForecasts[0].Location.TimePeriods.Daily[].Time[].{ DateTime, Tide }
```

原解析碼處理陣列時只取到 `tf[0]`（即 `{ Location: {…} }`），少進了 `.Location` 這一層，導致 `periods` 永遠是 `undefined`，解析不到任何資料。

**修改前：**

```js
const tf = records.TideForecasts;
const loc = tf.Location || tf.location || (Array.isArray(tf) ? tf[0] : null);
// 陣列時 loc = tf[0] = { Location: {...} }
const periods = loc.TimePeriods;   // ✗ undefined（少了一層 .Location）
```

**修改後：**

```js
const tf = records.TideForecasts;
const wrapper = Array.isArray(tf) ? tf[0] : tf;     // 先取陣列第一筆
const loc = wrapper?.Location || wrapper?.location || wrapper;  // 再進到 .Location
const periods = loc.TimePeriods || loc.timePeriods; // ✓ 正確
```

- **位置**：`index.html` — `fetchTideData()` 內「路徑 B」解析區塊
- **處理**：已修正，正確深入到 `TimePeriods.Daily`

---

## 問題 4：CONFIG 的日期已過期（使用時再改）

`CONFIG.date` 設為 `2025-05-24`，但目前已是 2026-06-11。F-A0021-001 只提供**未來 1 個月**的潮汐，程式用 `dt.startsWith(targetDate)` 比對日期，一個一年多前的日期不可能出現在回應裡 → 一定比對不到。

- **位置**：`index.html` — `CONFIG.date`
- **本質**：這是**設定值**問題，不是程式 bug
- **處理**：實際使用時把 `date` 設成**活動當天，且需落在未來一個月內**

---

## 後續啟用步驟（要真正抓資料時）

1. 申請並填入 `CONFIG.cwaApiKey`（問題 1）
2. 將 `CONFIG.date` 設為活動當天、且在未來一個月內（問題 4）
3. 開啟頁面後查看瀏覽器 Console 的「🌊 潮汐 API 原始回應」前 500 字，核對實際回應結構是否與解析邏輯一致；若有差異再微調

---

## 參考來源

- [F-A0021-001 潮汐預報資料集（CWA 開放資料平臺）](https://opendata.cwa.gov.tw/dataset/observation/F-A0021-001)
- [潮汐預報-未來1個月潮汐預報（政府資料開放平臺）](https://data.gov.tw/dataset/7038)
